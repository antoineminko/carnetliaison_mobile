import 'package:flutter/material.dart';

class HomeworkPreview extends StatelessWidget {
  const HomeworkPreview({super.key});
  @override
  Widget build(BuildContext context) {
    return const Card(child: Text('Homework Preview'));
  }
}
