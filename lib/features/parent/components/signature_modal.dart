import 'package:flutter/material.dart';

class SignatureModal extends StatelessWidget {
  const SignatureModal({super.key});
  @override
  Widget build(BuildContext context) {
    return const Dialog(child: Text('Signature Modal'));
  }
}
