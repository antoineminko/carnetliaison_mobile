import 'package:flutter/material.dart';

class SignaturePad extends StatelessWidget {
  const SignaturePad({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(child: Text('Signature Pad'));
  }
}
