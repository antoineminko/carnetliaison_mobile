import 'package:flutter/material.dart';

class HomeworkManagerPage extends StatelessWidget {
  const HomeworkManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Homework Manager Page')));
  }
}
