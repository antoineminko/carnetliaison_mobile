import 'package:flutter/material.dart';

class DocumentViewer extends StatelessWidget {
  const DocumentViewer({super.key});
  @override
  Widget build(BuildContext context) {
    return const SizedBox(child: Text('Document Viewer'));
  }
}
